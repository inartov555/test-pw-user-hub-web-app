[pytest]
addopts = -ra -q -n auto --maxfail=1
testpaths = tests
markers =
    smoke: quick smoke checks
    regression: full regression suite
    slow: slow tests (file uploads, heavy flows)
filterwarnings =
    ignore::DeprecationWarning
